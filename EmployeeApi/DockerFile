# 1️ Use the official .NET 8 SDK image for building the app
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /app

# 2️ Copy the project file and restore dependencies
COPY *.csproj ./
RUN dotnet restore

# 3 Copy the remaining source code and publish the app
COPY . ./
RUN dotnet publish -c Release -o out

# 4 Use the official .NET 8 runtime image
FROM mcr.microsoft.com/dotnet/aspnet:8.0
WORKDIR /app

# 5️ Copy the published app from the build stage
COPY --from=build /app/out .

# 6️ Expose port (the port your API listens on)
EXPOSE 5120

# 7 Set the entry point to run the app
ENTRYPOINT ["dotnet", "EmployeeApi.dll"]
